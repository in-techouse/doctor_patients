class HelloDocMessage {
  String id, fromUser, toUser, text, chatId;
  var timeStamps;

  HelloDocMessage(
      {this.id,
      this.fromUser,
      this.toUser,
      this.text,
      this.chatId,
      this.timeStamps});
}
